# better-sqlite3 ships a compiled binary tied to Node's ABI, so installing or
# running under a different major fails at runtime with a confusing
# NODE_MODULE_VERSION error. Turn the engines field in package.json into a hard
# stop rather than a warning.
engine-strict=true
